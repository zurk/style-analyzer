# Report for the commits with typo dataset

## Metrics

| Metric         |  Value   |
|---------------:|:---------|
{% set scores_to_show = ["detection_precision", "detection_recall", "fix_accuracy",
                         "top3_detection_precision", "top3_detection_recall", "top3_fix_accuracy",
                         "support", "review_time"] %}
{% for score_name in scores %}
| {{ "%24s" % score_name}} | {{ "%.3f" % scores[score_name]}} |
{% endfor %}

{%  if fails|length != 0 %}
Report generation failed for
{% for row_index in failures %}
{{ row_index }}. {{ failures[row_index]["repo"] }}
{% endfor %}
{% endif %}

## Versions

* Style-analyzer package version is {{ package_version }}
* Commit is {{ commit if commit is not none else "N/A" }}
